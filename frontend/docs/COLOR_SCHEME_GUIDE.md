# 网站配色方案修改指南

## 📋 配色系统总览

网站采用**两层配色架构**：

### 第 1 层：CSS 变量（推荐）✅

在 `src/index.css` 中定义，易于全局修改

### 第 2 层：硬编码颜色值（需要逐一修改）

散布在各个 `.css` 文件中，不够灵活

---

## 🎨 核心配色色板

当前默认配色：

| 名称   | 十六进制  | RGB                | 用途                   |
| ------ | --------- | ------------------ | ---------------------- |
| 主色   | `#667eea` | rgb(102, 126, 234) | 按钮、链接、边框、强调 |
| 次色   | `#764ba2` | rgb(118, 75, 162)  | 悬停效果、渐变终点     |
| 文本   | `#333333` | rgb(51, 51, 51)    | 主要文本               |
| 浅文本 | `#666666` | rgb(102, 102, 102) | 辅助文本               |
| 边框   | `#e0e0e0` | rgb(224, 224, 224) | 分割线、输入框边框     |
| 浅背景 | `#f9f9f9` | rgb(249, 249, 249) | 卡片背景、表单背景     |

---

## 🔧 修改步骤

### 方案 A：快速修改（只需改 2 个地方）⭐ 推荐

#### 1️⃣ 修改 CSS 变量（`src/index.css`）

```css
:root {
  /* 颜色变量 */
  --primary-color: #667eea; /* 改这里 - 主色 */
  --secondary-color: #4ba296; /* 改这里 - 次色 */
  --text-color: #333;
  --text-light: #666;
  --border-color: #e0e0e0;
  --bg-light: #f9f9f9;
}
```

**说明：**

- `--primary-color`: 主按钮、链接、文本输入框边框
- `--secondary-color`: 悬停效果、渐变终点

#### 2️⃣ 修改硬编码颜色值

由于设计需要，某些色值无法使用 CSS 变量，需要在以下文件中逐一修改：

### 方案 B：完整修改（精细控制）

如果要彻底改造配色，需要修改以下文件：

---

## 📁 需要修改的文件详细表

### 1. `src/index.css` - 根 CSS 变量

**优先级：🔴 最高**

```css
:root {
  --primary-color: #667eea; /* 主色 */
  --secondary-color: #4ba296; /* 次色 */
  --text-color: #333; /* 文本色 */
  --text-light: #666; /* 浅文本 */
  --border-color: #e0e0e0; /* 边框色 */
  --bg-light: #f9f9f9; /* 浅背景 */
}
```

**影响范围：**

- ✅ 表单输入框边框
- ✅ 表单焦点阴影
- ✅ 表单按钮样式
- ✅ 复选框颜色

---

### 2. `src/styles/common.css` - 通用样式

**优先级：🟠 高**

需要修改的颜色：

```css
/* 第20行：h1 渐变背景 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                              ↓                ↓
                           主色              次色

/* 第31行：h2 底部边框 */
border-bottom: 3px solid #667eea;
                          ↓
                       主色

/* 第36行、43行：链接颜色 */
color: #667eea;      /* 正常 */
color: #764ba2;      /* 悬停 */
```

---

### 3. `src/styles/button.css` - 按钮样式

**优先级：🟠 高**

```css
/* 第17-19行：主按钮 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
                            ↓       ↓       ↓
                           RGB 值（主色）

/* 第24行：悬停阴影 */
box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);

/* 第29-30行：次按钮 */
color: #667eea;           /* 文本色 */
border: 2px solid #667eea; /* 边框色 */

/* 第41、46行：禁用阴影 */
box-shadow: 0 2px 8px rgba(102, 126, 234, 0.1);
```

---

### 4. `src/styles/form.css` - 表单样式

**优先级：🟠 高**

```css
/* 第15-19行：拖拽区域默认状态 */
border: 2px dashed #667eea;
background: rgba(102, 126, 234, 0.05);
                  ↓       ↓       ↓
                 RGB 值（主色，5% 透明度）

/* 第25-26行：拖拽区域悬停 */
border-color: #764ba2;
background: rgba(118, 75, 162, 0.08);
              ↓       ↓       ↓
             RGB 值（次色，8% 透明度）

/* 第30-31行：拖拽区域激活 */
border-color: #764ba2;
background: rgba(118, 75, 162, 0.12);

/* 第66、73行：拖拽文本 */
color: #667eea;      /* 默认 */
color: #764ba2;      /* 文件数 */

/* 第102、163行：输入框边框 */
border-color: #667eea;

/* 第164行：焦点阴影 */
box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);

/* 第222、237行：复选框 */
accent-color: #667eea;
background-color: #667eea;
```

---

### 5. `src/styles/checkbox.css` - 复选框

**优先级：🟡 中**

```css
/* 第25、40行：复选框颜色 */
accent-color: #667eea;
background-color: #667eea;
```

---

### 6. `src/styles/layout.css` - 布局及表单组件

**优先级：🟡 中**

```css
/* 第79、112、138行：焦点/按钮背景 */
--primary-color: #007bff; /* 这里用变量 */

/* 第149行：次按钮背景 */
--secondary-color: #6c757d; /* 这里用变量 */
```

**注意：** 这个文件已部分使用 CSS 变量，建议保持不变

---

### 7. `src/styles/home.css` - 首页样式

**优先级：🟡 中**

```css
/* 第5行：渐变背景 */
background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
                                      ↓                           ↓

/* 第7行：边框 */
border: 1px solid rgba(102, 126, 234, 0.2);

/* 第36-37行：阴影和边框 */
box-shadow: 0 12px 24px rgba(102, 126, 234, 0.15);
border-color: #667eea;
```

---

### 8. `src/styles/about.css` - 关于页面

**优先级：🟡 中**

```css
/* 第6行：左边框 */
border-left: 4px solid #667eea;

/* 第41行：渐变背景 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 第47行：阴影 */
box-shadow: 0 2px 8px rgba(102, 126, 234, 0.2);

/* 第73-74行：背景和文本 */
background: rgba(102, 126, 234, 0.1);
color: #667eea;
```

---

### 9. `src/styles/message.css` - 消息提示

**优先级：🟢 低**（使用专用错误/成功颜色，不用改主色）

```css
/* 错误消息 */
.error-box {
  background: #ffebee; /* 浅红背景 */
  border-left-color: #f44336; /* 红色边框 */
  color: #c62828; /* 深红文本 */
}

/* 成功消息 */
.success-box {
  background: #e8f5e9; /* 浅绿背景 */
  border-left-color: #4caf50; /* 绿色边框 */
  color: #2e7d32; /* 深绿文本 */
}
```

---

### 10. `src/styles/404.css` - 404 页面

**优先级：🟢 低**

需要检查是否有硬编码颜色

---

## 🎯 常见配色方案

### 方案 1：深蓝紫（当前）

```
--primary-color: #667eea
--secondary-color: #764ba2
```

### 方案 2：绿色系

```
--primary-color: #10b981    /* 绿色 */
--secondary-color: #059669  /* 深绿 */
```

### 方案 3：橙色系

```
--primary-color: #f59e0b    /* 橙色 */
--secondary-color: #d97706  /* 深橙 */
```

### 方案 4：红色系

```
--primary-color: #ef4444    /* 红色 */
--secondary-color: #dc2626  /* 深红 */
```

### 方案 5：青色系（现代）

```
--primary-color: #06b6d4    /* 青色 */
--secondary-color: #0891b2  /* 深青 */
```

---

## 🔄 RGB 到十六进制转换表

当修改色值时，如果需要调整透明度的 RGB 值：

| 主色    | RGB             | 说明     |
| ------- | --------------- | -------- |
| #667eea | (102, 126, 234) | 标准蓝紫 |
| #764ba2 | (118, 75, 162)  | 深紫     |

**透明度计算示例：**

```
rgba(102, 126, 234, 0.1)  = 10% 透明度（浅背景）
rgba(102, 126, 234, 0.05) = 5% 透明度（极浅背景）
rgba(102, 126, 234, 0.15) = 15% 透明度（阴影）
```

---

## ✅ 修改清单

### 快速改色（推荐 5 分钟）

- [ ] 修改 `src/index.css` 中的 `--primary-color` 和 `--secondary-color`
- [ ] 测试网站，查看效果

### 完整改色（详细 30 分钟）

- [ ] 修改 `src/index.css` 中的 CSS 变量
- [ ] 修改 `src/styles/common.css` 中的硬编码颜色
- [ ] 修改 `src/styles/button.css` 中的硬编码颜色
- [ ] 修改 `src/styles/form.css` 中的硬编码颜色
- [ ] 修改 `src/styles/checkbox.css` 中的硬编码颜色
- [ ] 修改 `src/styles/home.css` 中的硬编码颜色
- [ ] 修改 `src/styles/about.css` 中的硬编码颜色
- [ ] 在浏览器中测试所有页面

---

## 🚀 建议改进

为了使配色更加灵活，建议：

1. **将所有硬编码颜色迁移到 CSS 变量**
2. **创建主题预设文件** `src/styles/themes.css`
3. **实现主题切换功能**（亮色/暗色模式）

### 示例：创建主题预设

```css
/* src/styles/themes.css */
:root[data-theme="default"] {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}

:root[data-theme="green"] {
  --primary-color: #10b981;
  --secondary-color: #059669;
}

:root[data-theme="orange"] {
  --primary-color: #f59e0b;
  --secondary-color: #d97706;
}
```

---

## 📞 快速参考

**最重要的 2 个变量：**

```
--primary-color   # 主色（按钮、链接、边框）
--secondary-color # 次色（悬停效果）
```

**需要逐一修改的 7 个文件：**

1. `common.css` - 标题和链接
2. `button.css` - 按钮样式
3. `form.css` - 表单和拖拽区
4. `checkbox.css` - 复选框
5. `home.css` - 首页容器
6. `about.css` - 关于页面
7. `layout.css` - 可选（已用变量）
